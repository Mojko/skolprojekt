using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class NPC_5000 : NPCMain {

    /*public override void execute(int state)
    {
        if(state == 0) {
            //Say some text
            this.sendNext("Hello there");
        }
        if(state == 1) {
            //Say some text
            this.sendYesNo("Can you go collect this item for me");
            if(this.selection == 0){ } // Selected no!
            if(this.selection == 1){ } //Selected yes!
        }
    }*/
}
